apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "data-upload-webapp-chart.fullname" . }}-test-connection"
  labels:
    {{- include "data-upload-webapp-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "data-upload-webapp-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
