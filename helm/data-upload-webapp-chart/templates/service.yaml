apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-upload-webapp-chart.fullname" . }}
  labels:
    {{- include "data-upload-webapp-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "data-upload-webapp-chart.selectorLabels" . | nindent 4 }}
